package spd.trello.repository;

import org.springframework.stereotype.Repository;
import spd.trello.domain.BoardTemplate;


@Repository
public interface BoardTemplateRepository extends AbstractRepository<BoardTemplate> {
}